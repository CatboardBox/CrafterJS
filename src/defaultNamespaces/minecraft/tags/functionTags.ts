import { FunctionTag } from "../../../classes";
import { namespace } from "../namespace";

export const onLoad = new FunctionTag("onLoad", namespace);
export const onTick = new FunctionTag("onTick", namespace);
